node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Electron installers — upload manually via deploy script
public/downloads/*.exe
public/downloads/*.dmg
public/downloads/*.AppImage
public/downloads/*.apk

# Chave de assinatura do APK — nunca commitar (perder = quebra atualização do app instalado)
android-keystore/

# App desktop Electron — instaladores gerados manualmente, não versionar (ficam em public/downloads/)
pdv-desktop/dist-installer/
